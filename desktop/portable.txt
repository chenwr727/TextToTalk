TextToTalk 免安装版

这是免安装（portable）版本的标记文件，请勿删除。

存在此文件时，应用会把配置、成片、缓存等全部数据保存在本文件
同级的 UserData 目录下，而不写入系统的应用数据目录（%APPDATA%）。
整个文件夹可以复制到 U 盘或另一台电脑，数据会跟着一起走。

如果不希望这样（想让数据回到 %APPDATA%），删掉本文件即可；
之后已有的数据不会丢失，只是会重新在系统目录里生成一份。
